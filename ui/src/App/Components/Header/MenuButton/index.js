import React from 'react';
import MenuButtonImage from '../../../Resources/MenuButton.png';
import { MenuButtonIcon } from './styles';
function MenuButton(props) {
    return <MenuButtonIcon src={MenuButtonImage} />
}

export default MenuButton;
